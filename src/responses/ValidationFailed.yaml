description: "Validation failed"
content:
  application/json:
    schema:
      $ref: "../schemas/Error.yaml"
    example:
      message: "Validation failed"
      code: 422
      details: |
        The request was successfully constituted but could not be processed due to semantic errors.
        If you try to add an address, the address may not be valid.
